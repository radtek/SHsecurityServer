import EventData from '../event/EventData';

export default class SocketEventData extends EventData {
    
            // public constructor(messageID: string, messageData?: ByteBuffer) {
            //     super(messageID, messageData);
            // }
}